#ifndef CORE_HPP
#define CORE_HPP

#include <cmath>
	
#define MAXNUM_AGENT 10

struct Terminal2Gazebo_info
{
    int map_height_;                                       //the height of map
    int map_width_;                                        //the width of map
    int map_room_;                                         //the room number of map
    bool ismap_recreate_;                                  //is recreate the map
};

struct Terminal2Robots_info
{

};

#endif //CORE_HPP
